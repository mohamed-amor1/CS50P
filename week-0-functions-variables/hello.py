name = input("What's your name? ")
print(f"Hello, {name}")

# The f in print(f"Hello, {name}") is used to create an f-string, which allows for the embedding of expressions and variables inside string literals in a more readable and convenient way.
