# Import the statistics module, which contains a variety of functions for calculating statistics
import statistics

# Call the mean() function from the statistics module to calculate the arithmetic mean of the values in a list
# The list contains the values 100 and 90
mean_value = statistics.mean([100, 90])

# Print out the calculated mean value
print(mean_value)
