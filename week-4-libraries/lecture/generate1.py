# Import the random module, which contains a variety of functions for generating random numbers and values
import random

# Call the randint() function from the random module to generate a random integer between 1 and 10 (inclusive)
# and assign it to the variable named "number"
number = random.randint(1, 10)

# Print out the value of the "number" variable
print(number)
